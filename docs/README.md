# 支付资金底座设计文档总入口

## 目录定位

本目录承载支付资金底座的最终版设计基线，用于产品评审、系分设计、DSL 契约承接、测试驱动设计和生产准入评审。

本目录只保留最终设计口径，不承载任务拆解、历史材料或变更说明；交付评审以本文档包完成代码、测试、DDL 和现有实现差距复核。

## 权威设计包

| 目录 | 定位 | 主要读者 | 产出边界 |
| --- | --- | --- | --- |
| [产品设计](产品设计/README.md) | 产品 PRD 和业务架构基线。 | 产品、运营、财务、风控、研发、测试。 | 背景、目标、角色、对象、流程、规则、能力边界、验收和风险。 |
| [DSL设计](DSL设计/README.md) | 产品语义到系分和代码契约之间的承载层。 | 架构、研发、测试、产品。 | 资金事实、资金指令、路由快照、账务计划、分录、投影和 JSON 契约。 |
| [系分设计](系分设计/README.md) | 面向编码落地的系统分析设计。 | 架构、研发、测试、SRE、安全。 | 模块边界、服务能力、表设计、状态机、一致性、可观测、安全和准入门禁。 |
| [TDD设计](TDD设计/README.md) | 把产品验收和 DSL 契约转成测试驱动设计。 | 测试、研发、架构、产品。 | 测试分层、场景矩阵、红线用例、真实执行路径、断言证据和验证命令。 |

## 图形化产物

图形化产物用于快速评审产品资金路径、系统分层和关键交易流程，不替代 PRD、DSL、系分或 TDD 正文契约。评审时先用图确认主链路，再回到正文分册核对对象、字段、状态、红线和测试证据。

| 图 | 文件 | 用途 |
| --- | --- | --- |
| 产品核心资金流 | [prd-core-funds-flow.svg](图形化/prd-core-funds-flow.svg) | 从产品视角串起交易、路由、余额控制、账务、投影和对账关系。 |
| 系统核心架构 | [system-core-architecture.svg](图形化/system-core-architecture.svg) | 从系统视角展示产品门面、交易编排、路由、账务、投影、归档和观测分层。 |
| 直接交易流程 | [direct-transaction-flow.svg](图形化/direct-transaction-flow.svg) | 辅助核对直接交易从请求、路由、账务到投影的交付链路。 |
| 授权交易流程 | [authorization-transaction-flow.svg](图形化/authorization-transaction-flow.svg) | 辅助核对授权、完成、撤销、过期、退款、拒付和路由快照边界。 |
| 余额控制流程 | [balance-control-flow.svg](图形化/balance-control-flow.svg) | 辅助核对冻结、解冻、余额调整、额度调整和失败无副作用边界。 |

## 推荐阅读顺序

```mermaid
flowchart LR
    PRD["产品设计\n为什么做 / 给谁用 / 做到什么边界"] --> DSL["DSL 设计\n业务事实如何结构化承载"]
    DSL --> SAD["系分设计\n模块 / 接口 / 数据 / 状态 / 观测"]
    SAD --> TDD["TDD 设计\n用例 / 红线 / 断言 / 验证命令"]
    TDD --> Gap["交付差距复核\n代码 / 测试 / DDL / 现有行为"]
```

## 跨文档追踪关系

| 从 | 到 | 追踪重点 |
| --- | --- | --- |
| 产品设计 | DSL 设计 | 产品场景是否能落到资金事实、主体、账户、账目、路由、账务和投影。 |
| DSL 设计 | 系分设计 | DSL 对象、事件和不变量是否有模块、接口、表结构和状态机承接。 |
| 产品设计 | 系分设计 | 产品流程和能力边界是否被拆成清晰的系统职责、服务能力和数据边界。 |
| 产品设计 / DSL 设计 / 系分设计 | TDD 设计 | 每个资金变化是否能被状态、余额桶、账本分录、投影、幂等和异常路径证明。 |

## 设计准入评估总控

本设计包用于产品和开发团队做进入编码设计、TDD 拆解和差距复核前的准入评估，不等同于上线批准、合规结论、财务处理结论、税务判断或外部机构规则确认。涉及真实资金、客户资金、商户待结算资金、支付资质、备付金、反洗钱、个人信息、卡组织、银行、清算网络、跨境或外汇规则时，必须由法务、合规、财务、风控、数据安全或通道负责人给出最终确认。

### 设计 CR 优化口径

后续评审不要只问“功能有没有设计完”，而要按下列顺序检查设计是否真的可被生产、运营和测试使用。任一项无法回答时，优先补产品、DSL、系分或 TDD 证据，不进入编码授权。

| 顺序 | CR 视角 | 必须回答的问题 | 优化方向 |
| --- | --- | --- | --- |
| 1 | 可用性 | 这条能力在正常、失败、重试、人工处理和只读查询下是否都有明确入口和终态。 | 补齐主流程、逆向流程、失败终态、人工兜底、查询口径和 Runbook 触发条件。 |
| 2 | 资金安全 | 是否能证明不重复入账、不半截入账、不绕过账本、不跨主体误转、不把投影当事实源。 | 补 route snapshot、posting plan、ledger entry、余额桶、幂等键、唯一约束、审批和审计证据。 |
| 3 | 金融红线 | 是否存在资质、客户资金、商户待结算资金、外部规则、敏感数据或合规确认缺口。 | 写清待确认方、确认项、未确认前不覆盖范围和必须失败场景。 |
| 4 | 易用性 | 用户、商户、运营、财务、风控和客服能否理解“发生了什么、为什么、下一步怎么办”。 | 补用户账单、商户账单、运营时间线、错误原因、审批状态、差错责任和处理建议。 |
| 5 | 可理解性 | 同一概念在 PRD、DSL、系分和 TDD 中是否同名同义，状态、事件、账目和周期是否不会混用。 | 回到统一术语映射、caseId、AC/TDD/RED 映射和测试命名，消除同词异义。 |

评估必须同时覆盖下列维度：

| 维度 | 产品设计要回答 | DSL 设计要回答 | 系分设计要回答 | TDD 设计要回答 | 阻断信号 |
| --- | --- | --- | --- | --- | --- |
| 设计可用性 | 场景、角色、目标、非目标和人工处理是否清楚。 | 业务事实、事件类型、主体、金额和引用是否可结构化。 | 服务入口、状态机、表设计和异常路径是否能落地。 | 是否能转成正常、异常、边界、幂等和并发用例。 | 只能描述“成功/失败”，无法解释输入事实、资金路径、状态变化和后续处理。 |
| 资金安全 | 客户资金、商户待结算资金、平台自有资金、手续费、补贴、成本和保证金是否隔离。 | 可记账主体、route snapshot、posting plan、LedgerEntry 是否能证明路径。 | 事务、唯一约束、幂等、审批、审计和失败无副作用是否明确。 | 是否断言余额桶、账本周期、分录平衡、投影一致和红线失败。 | 外部账户入账、平台账户角色混用、投影或后台改余额、重复出款、缺审批调账。 |
| 金融红线 | 法域、资质、客户资金、KYC/KYB/AML、敏感信息和外部规则是否列为待确认。 | DSL 是否保留规则来源、版本、确认方、证据引用和脱敏边界。 | 权限、审计、敏感字段、导出、外部协议和生产红线是否落地。 | 是否有敏感信息、外部账户、争议证据和合规待确认的 must-fail 用例。 | 把内部清分清算写成持牌清算结论；把待结算资金泛称客户备付金；绕过 KYC/KYB/AML；保存完整 PAN、CVV、密钥或明文敏感凭证。 |
| 易用性 | 用户、商户、运营、财务和风控能否理解余额、账单、冻结、授权、清算、退款和争议状态。 | DSL 字段是否能支撑可解释展示和操作原因。 | 查询、投影、时间线、错误原因和人工处理入口是否完整。 | 是否覆盖用户账单、商户账单、运营时间线和失败提示断言。 | 授权展示成最终消费、冻结展示成扣款、待清算展示成可提现、争议退款被普通退款吞掉。 |
| 可理解性 | 概念是否稳定，术语是否和总入口统一映射一致。 | 枚举、事件、状态和 JSON 示例是否避免同词异义。 | 模块、表、状态和 API 命名是否承接产品/DSL。 | 测试命名是否能反查产品验收和 DSL 契约。 | 授权完成和商户结算混用，账本周期、清算账期、报表周期、归档水位或规则窗口混用。 |
| 可开发性 | 能力边界、强规则、配置项、待确认项和非目标是否明确。 | 契约对象、不变量和失败边界是否足够稳定。 | 模块边界、依赖方向、数据模型、事务边界和观测指标是否可实施。 | 目标测试资产、真实执行路径和验证命令是否明确。 | 规则只存在口头描述，失败只能人工改库，公共契约需要边做边改但无 Execution Grant。 |
| 可测试性 | 验收矩阵是否覆盖主流程、逆向、异常、人工、报表和红线。 | 契约样例是否能驱动断言。 | 系分是否提供状态、表、服务和观测落点。 | TDD 是否能证明状态、金额、route、posting、entry、projection、audit 和 no-side-effect。 | 只断言接口不报错、交易状态或 entry 数量，不断言余额桶、分录平衡和幂等副作用。 |

准入结论只允许三种：

| 结论 | 含义 | 后续动作 |
| --- | --- | --- |
| 通过 | 四类设计入口在本次范围内闭合，资金路径、契约、系统落点和 TDD 断言都可追溯。 | 进入交付差距复核，并在 Execution Grant 中声明允许触碰范围。 |
| 带条件通过 | 主链路可拆解，但存在不影响核心资金不变量的待确认项。 | 写清条件、负责人、补齐时点和不覆盖范围；编码任务不得越过待确认边界。 |
| 阻断 | 存在资金安全、金融红线、主体不清、路径不可追溯、契约不稳、验收不可测或外部规则未确认的关键缺口。 | 暂停编码，先补产品、DSL、系分、TDD 或合规/财务/通道确认。 |

### 设计交付评分复核口径

设计交付评分不是文档篇幅评估，而是准入风险评估。低于 90 分的维度必须回到本表补证据；补证据后仍无法闭合时，准入结论只能是带条件通过或阻断。

| 维度 | 90 分以上必须具备的证据 | 常见扣分点 | 补强入口 |
| --- | --- | --- | --- |
| 金融红线 | 外部规则、合规、会计、税务、敏感数据、客户资金和持牌事项都有确认方、确认时点和不覆盖范围。 | 只写“待确认”，未说明谁确认、确认什么、未确认前不能做什么。 | 产品设计 01 的待确认项和本文件生产准入检查清单。 |
| 系分可开发性 | 每个实现切片都有服务入口、表职责、状态、幂等键、事务边界、观测指标和失败副作用。 | 表设计过粗、接口和状态落点不清、实现时需要临时决定公共契约。 | 系分 README 的实现承接清单和各分册表设计章节。 |
| TDD 可验证性 | 每个批次有 AC/DSL/TDD/RED 显式映射、最小断言集、失败无副作用和验证命令。 | 只说明测试范围，未说明要证明哪些资金事实。 | TDD 设计的覆盖证明卡和测试证据链。 |
| 可理解性 | 评审角色能按固定路径读文档，并能用同一术语解释产品、DSL、系分和 TDD。 | 同一概念多名、周期混用、授权完成和商户结算混用。 | 本文件统一术语映射和各 README 阅读顺序。 |
| 交付门禁 | 编码前有批次准入卡，明确范围、允许触碰项、TDD 清单、验证命令和待确认不覆盖范围。 | 跳过 Execution Grant，直接把设计包当作全量编码授权。 | OpenSpec Harness Plan 和下方批次准入卡。 |

## 按角色使用路径

不同角色使用本设计包时关注点不同，但最终都必须回到同一条证据链：产品场景、DSL 契约、系分落点和 TDD 断言。

快速评审时先读本文件的准入评估总控、统一术语映射和编码批次准入卡；再按角色进入对应分册。不要从任意长表开始评审，否则容易只看到字段或用例，忽略资金语义和红线。

| 使用者 | 先读 | 必须拿到的结论 | 不应跳过 |
| --- | --- | --- | --- |
| 产品和业务负责人 | 产品设计/01、产品设计/05 | 场景是否真实、目标是否清楚、哪些能力属于默认主链路、哪些是扩展或外部待确认。 | 概念边界、非目标、产品红线和验收矩阵。 |
| 运营、财务和风控 | 产品设计/03、产品设计/05、系分设计/05 | 异常如何阻断、清结算和对账如何闭环、谁有权限处理、处理后如何留痕。 | 差错单、审批、凭证、核销、重新对账和追偿链路。 |
| 架构和研发 | DSL设计、系分设计/01、相关系分分册 | 资金事实如何进入 DSL，落到哪些模块、服务、表、状态机和事务边界。 | 账本事实源、route snapshot、幂等键、唯一约束和金融红线。 |
| 测试 | 产品设计/05、TDD设计、系分设计/05 | 每个场景的正常、异常、边界、并发和 must-fail 用例如何落到测试资产。 | 余额桶断言、posting plan 平衡、ledger entry、投影和失败无副作用。 |
| 安全、合规和审计 | 产品设计/01、系分设计/05、外部参考清单 | 敏感数据、权限、导出、审批、外部规则和合规待确认项是否清楚。 | 本设计不替代法务、税务、会计、合规或持牌机构最终确认。 |

## 统一术语映射

授权链路和商户结算链路都会出现“结算”字样。为了避免产品、DSL、系分和测试之间混用，本文档包采用以下映射：

| 产品表达 | DSL / 系分 / TDD 表达 | 语义边界 |
| --- | --- | --- |
| 授权结算 | 授权完成，事件使用 `AUTHORIZATION_TRANSACTION / SETTLE`。 | 指授权占用转为实际消费、扣款或待清算结果；不等同于商户结算单或出款。 |
| 授权部分结算 | 授权部分完成，事件仍使用 `SETTLE`，以原授权为引用。 | 指只完成部分授权金额；累计完成金额不得超过原授权金额。 |
| 已结算金额 | 授权链路中优先称为已完成金额。 | 指原授权已经完成的累计金额；只用于约束退款、拒付、过期释放和迟到事件。 |
| 结算后退款 | 授权完成后退款，事件使用 `AUTH_REFUND` 或对应退款链路。 | 必须基于原授权完成路径反向，不按当前绑定关系重新选路。 |
| 授权撤销 | 外部明确撤销或冲正，事件使用 `REVERSAL`。 | 释放剩余授权占用，终态为撤销；不能和系统过期混成同一结果。 |
| 授权过期 | 系统根据授权有效期释放剩余占用，事件使用 `EXPIRE`。 | 可复用授权释放账务路径，但状态、释放原因、审计和过期金额必须保留 `EXPIRE` 语义。 |
| 无前置授权强制完成 | 使用 `SETTLE` 的强制完成模式。 | 不新增独立强制完成事件，不伪造授权占用；必须有策略、上限、原因和审计。 |
| 通道预处理与事件编排类场景 | 上层业务或通道适配层归一后的场景。 | 不作为资金底座默认 DSL 和系分场景；资金底座只接收明确资金事实。 |
| 结算锁定 | 清结算链路的 `AVAILABLE -> SETTLEMENT`。 | 指商户、合作方或企业出款前锁定可结算余额；不代表外部出款成功。 |
| `SETTLEMENT` 账目 | 出款中或结算处理中锁定资金。 | 是余额桶，不是授权生命周期的 `SETTLE` 事件。 |

## 生产准入检查清单

| 检查项 | 准入要求 |
| --- | --- |
| 文档范围 | 最终设计只以 产品设计、DSL设计、系分设计、TDD设计 为入口。 |
| 最终口径 | 最终文档不得依赖历史材料、临时任务记录或变更说明。 |
| 版本控制 | 最终设计目录下的新文档必须能被 Git 正常追踪，不能因 ignore 规则丢失。 |
| 术语一致 | 产品表达、DSL 事件、系分状态和 TDD 用例必须能按“统一术语映射”互相解释。 |
| 场景闭合 | 正向、逆向、异常、人工处理、幂等、重放、对账差错和红线用例必须有验收承接。 |
| 资金红线 | 不得绕过账本事实、不得直接改余额投影、不得用日志或报表修复余额、不得混同冻结和消费。 |
| 指标边界 | 本设计只列产品和业务关心的资金指标项；指标计算、存储和报表实现由报表指标模块承接。 |
| 外部规则 | 外部机构、卡组织、ACH、银行、PSP、Highnote、Stripe 等公开资料只作参考；上线前必须确认版本、法域、生效日期、确认方和确认结论。 |
| 编码入口 | 进入编码实现必须先做代码、测试、DDL 和现有行为差距复核，再拆分开发任务。 |

## 编码批次准入卡

任何编码批次开始前，必须把下列信息写入 Execution Grant 或等价任务说明。缺任一项时，不能把本设计包理解为编码授权。

| 项目 | 必填内容 |
| --- | --- |
| 批次目标 | 对应产品验收 ID、DSL caseId、系分分册、TDD 用例和红线编号。 |
| 写入范围 | 明确可改模块、可改文件类型、是否允许新增表、服务入口、枚举、Request/Query/DTO、状态机或公共契约。 |
| 只读范围 | 明确只允许阅读或复核的模块，避免顺手改动非本批次能力。 |
| 待确认边界 | 外部规则、合规、会计、税务、轨道/FX、报表指标、运营流程等未确认事项的不覆盖范围。 |
| 测试要求 | 先写或先恢复的测试、必须失败用例、余额断言、幂等断言和验证命令。 |
| 生产门禁 | 并发、锁、容量、回滚、补偿、审计、告警和人工处理要求；不涉及时写明不适用原因。 |
| 权益准入证明 | 含权益批次必须写清 Phase 能力边界、JSON 夹具级别、权益快照事实源、零实付表达、平台补贴表达、储值预付口径、退款分摊粒度、历史无快照处理和专业确认状态。 |

## 生产交付判定

本设计包当前结论是目标一致，可作为设计评审、差距复核和后续编码准入输入。生产交付必须按批次证明，不能因为文档齐备就替代已完成实现、已通过测试、已同步 DDL/H2 schema、已确认外部规则和已完成上线审批。若工作树存在未提交变更，未提交内容只作为草稿评审输入，提交或纳入 Execution Grant 前不得替代已冻结基线。

| 设计域 | 目标一致性 | 生产交付判定 | 进入生产交付前必须补齐 |
| --- | --- | --- | --- |
| 02 交易、路由、钱包、账目与投影 | 已对齐。PRD、DSL、系分和 TDD 都围绕资金事实、权益快照、route snapshot、posting plan、ledger entry、余额投影和审计证据链展开。 | 可进入批次 1 至批次 6 的差距复核和编码准入；权益金额组件进入编码时还需明确公共契约、枚举、route/posting/replay 消费、目标态范围和权益准入证明。 | 目标测试资产、余额断言、失败无副作用、幂等、公共契约授权、表结构、H2 schema、权益快照事实源、零实付表达、专业确认状态和 `fixtureLevel` 差距复核。 |
| 03 清结算与对账 | 已对齐。清分、清算、结算、出款、对账、差错、调账核销和追偿对象边界清楚。 | 仅达到设计评审输入态；编码准入未打开，当前不能声明数据库级或服务级生产交付完成。 | 独立 OpenSpec change、Execution Grant、DDL/H2 schema、Entity/Mapper、服务契约、状态机、NFR 假设、观测告警、对账和清结算服务级测试。 |
| 04 归档、重放与指标项边界 | 已对齐。归档 Manifest、checkpoint、watermark、差异报告、权益快照归档、交易投影重放和指标只读边界清楚。 | 仅达到设计评审输入态；已有局部交易投影重放基线不等于归档、余额快照和指标边界生产完成。 | 独立 Execution Grant、归档 Manifest、账本余额快照覆盖模式、权益快照冷热读取、指标水位隔离、范围锁、回滚/续跑、差异报告、NFR 假设、观测告警和边界测试。 |
| 外部规则、合规、税务、会计、轨道和敏感数据 | 已列出待确认项和不覆盖范围。 | 不作为生产启用结论。 | 规则版本、适用法域、生效日期、确认方、确认结论、审计和敏感数据处理方案。 |

### 跨文档生产证据链

生产交付不能只引用某一份文档。每个批次的交付说明必须能同时串起产品验收、DSL 契约、系分落点、TDD 断言、OpenSpec/Harness 授权和实际验证结果。

| 设计域 | PRD 证据 | DSL 证据 | 系分证据 | TDD 证据 | OpenSpec/Harness 证据 | 生产完成证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 02 交易、路由、钱包、账目与投影 | 产品 02、产品 05 的 AC/RED，含权益金额组件 AC-BEN 和权益准入红线。 | 直接交易、授权、余额控制、支付工具、权益快照、Route Replay、Posting/Ledger、投影相关 `DSL-*`。 | 系分 02 的服务契约、状态机、表设计、权益快照分期落点、编码准入决策卡、投影和红线章节。 | 批次 1 至批次 6 的目标测试资产、权益红线、权益准入证明和并发用例。 | OpenSpec spec、Harness 批次 1 至批次 6、Execution Grant 的 Phase/Fixture/事实源/零实付/专业确认字段。 | 代码、测试、H2 schema、验证命令、覆盖清单和残余风险均已列明。 |
| 03 清结算与对账 | 产品 03、产品 05 的清分、清算、结算、出款、对账、权益差错 AC/RED。 | 清结算确认、结算锁定、出款结果、权益金额项、对账差错调账和结算策略相关 `DSL-*`。 | 系分 03 的对象状态机、服务 API、表设计、权益金额项、阻断守卫、补偿和非功能。 | `TDD-CLS-*`、`TDD-SETTLE-*`、`TDD-RECON-*`、`TDD-BEN-CLS-*`、清结算并发和红线用例。 | 独立 OpenSpec change、Harness 批次 7、Execution Grant。 | DDL/H2、Entity/Mapper、服务实现、服务级 H2 流程测试、外部规则确认和人工处理闭环。 |
| 04 归档、重放与指标项边界 | 产品 04、产品 05 的归档、重放、权益快照、指标只读 AC/RED。 | 归档 Manifest、投影重放、权益快照归档、账本余额快照、指标快照边界相关 `DSL-*`。 | 系分 04 的资金数据治理逻辑边界、API、表设计、权益冷热读取、差异报告和只读红线。 | `TDD-ARCH-*`、`TDD-REPLAY-*`、`TDD-METRIC-*`、`TDD-BEN-REPLAY-*`、重放并发和红线用例。 | 独立 Execution Grant、Harness 批次 8。 | Manifest、checkpoint、watermark、范围锁、差异报告、dry-run/apply、回滚/续跑和边界测试。 |
| 外部规则和敏感数据 | PRD 01 待确认项和外部规则责任矩阵。 | 规则来源、版本、适用法域、确认方和脱敏字段。 | 系分 05 的权限、安全、审计和外部协议边界。 | 敏感数据、权限、导出、轨道/FX 和合规红线用例。 | Execution Grant 的待确认边界和人工确认点。 | 规则确认记录、脱敏验证、审计验证和未覆盖范围说明。 |

### 阻断和降级口径

交付评审中发现下列情况时，结论必须从“通过”降为“带条件通过”或“阻断”，不能写成普通优化项。

| 情况 | 结论 | 处理 |
| --- | --- | --- |
| 产品场景、DSL caseId、系分服务入口或 TDD 用例任一侧缺失。 | 阻断。 | 先补齐跨文档映射，再进入编码或验收。 |
| 有资金变化但未断言余额桶、posting plan、ledger entry、投影和幂等。 | 阻断。 | 补测试证据或缩小交付范围。 |
| 新增或修改公共契约、服务入口、枚举、Request/Query/DTO、状态机或表结构，但 Execution Grant 未授权。 | 阻断。 | 回到批次授权，明确允许范围和目标态语义。 |
| 含权益批次缺 Phase 能力边界、JSON 夹具级别、权益快照事实源、零实付表达、平台补贴表达、储值预付口径、退款分摊、历史无快照处理策略或专业确认状态。 | 阻断或降级为 contract-only。 | 补权益准入证明；未补前不得进入生产资金流 Done。 |
| 使用 `CONTRACT_ONLY` JSON 样例、请求态权益快照或 `contextVariables` 过渡字段作为 route/posting/replay、清结算或对账生产 Done 证据。 | 阻断。 | 改为资金流夹具、不可变事实源和服务级测试证据；或降级为契约承载。 |
| 清结算、对账、归档、重放或指标边界只有设计，没有 DDL/H2、服务级测试或边界测试。 | 设计评审可作为输入或带条件通过；生产完成或编码批次 Done 评审为阻断。 | 只能声明设计输入态；若目标是生产完成，必须先补实现和测试证据。 |
| 外部规则、合规、税务、会计、通道或敏感数据缺确认。 | 带条件通过或阻断。 | 写清确认方、补齐时点和不覆盖范围；影响资金释放或敏感数据时阻断。 |

### 结论映射

文档评审和编码批次使用不同结论词，必须按下表映射，不能把设计输入态或设计评审通过等同于生产完成。

| 中文评审结论 | OpenSpec/Harness 结论 | 使用边界 |
| --- | --- | --- |
| 通过 | 可进入 Done 评审。 | 设计闭环已成立；只有 Execution Grant 范围内的代码、契约、DDL/H2、测试、验证和待确认边界都闭合时，才能声明 Done。 |
| 带条件通过 | Conditional Done，或仅作为设计评审带条件。 | 主链路证据闭合，但外部规则、非本批次扩展、性能容量或人工确认项仍有明确不覆盖范围；若缺少本批生产目标必需的 DDL/H2、服务级测试或审计证据，则不能写 Conditional Done。 |
| 阻断 | Not Done。 | 资金红线、公共契约授权、DDL/H2、服务级测试、失败无副作用、幂等或审计证据缺失时使用；先补齐缺口再继续。 |

### 评审执行路径

评审时按下列步骤走，避免只看单份文档或只看测试结果。

| 步骤 | 动作 | 输出 |
| --- | --- | --- |
| 1. 确认范围 | 选择本次评审的设计域、批次、产品验收 ID、DSL caseId、TDD 用例和红线编号。 | 评审范围和非目标。 |
| 2. 检查设计闭环 | 从 PRD 反查 DSL、系分、TDD，再从 TDD 反查 PRD，确认没有孤立场景或孤立测试。 | 通过、带条件通过或阻断。 |
| 3. 检查授权边界 | 对照 Execution Grant，确认公共契约、服务入口、枚举、Request/Query/DTO、状态机、表结构、模块边界和生产行为是否被授权。 | 允许写入范围和禁止事项。 |
| 4. 检查实现证据 | 核对代码、DDL/H2 schema、服务契约、状态机、幂等、观测审计和架构边界证据。 | 实现证据包。 |
| 5. 检查测试证据 | 核对测试资产、核心断言、验证命令、结果、未覆盖项和残余风险。 | 测试证据包。 |
| 6. 给出结论 | 按阻断和降级口径判定通过、带条件通过或阻断；编码批次按 Done、Conditional Done、Not Done。 | 评审结论和后续动作。 |

## 资金安全总控

资金安全不是单一功能，而是贯穿产品、DSL、系分和 TDD 的共同控制。任一新增能力纳入最终设计或进入编码实现时，都必须能落到下列控制面。

| 控制面 | 必须成立 | 主要证据 |
| --- | --- | --- |
| 主体安全 | 入账主体只能是资金账户、信用账户、预算组或解析后的平台账户角色。 | PRD 概念边界、Route DSL、LedgerEntry 断言。 |
| 金额安全 | 金额为正、币种明确、精度合法、累计处理不超剩余可处理金额。 | DSL JSON、金额临界值测试、posting plan 平衡。 |
| 路径安全 | 正向交易按当前合法关系选路，逆向交易优先按原 route snapshot 回放。 | RouteSnapshot、RoutingDecision、Route Replay 测试。 |
| 账务安全 | 余额只能由账本分录派生，投影、日志、报表和指标不得反写事实。 | LedgerEntry、BalanceProjection、红线失败测试。 |
| 操作安全 | 调账、核销、结算锁定、出款、归档 apply 和敏感导出必须有权限、审批和审计。 | 操作日志、审批号、凭证、traceId、权限测试。 |
| 生产安全 | 清结算、对账、归档和重放必须有范围、幂等、差异报告、告警和人工兜底。 | 系分 03/04/05、TDD 并发和红线用例。 |

## 准入结论

本文档包可作为支付资金底座生产落地设计评审和交付差距复核的基线。若新增能力无法至少定位到产品场景、DSL 契约、系分模块或 TDD 断言中的一个入口，应先补设计归属；进入编码前还必须补齐产品场景、DSL 契约、系分模块和 TDD 断言的完整映射。
