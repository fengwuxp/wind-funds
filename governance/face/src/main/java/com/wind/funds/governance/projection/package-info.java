/**
 * 交易投影重放契约包。
 *
 * <p>本包只放置交易投影重放对外可见的应用服务、请求、范围、checkpoint、任务、结果和差异契约。
 * 它负责定义宿主如何创建、运行和查询投影重放任务，不承载内部事实来源、重建行、写入端口、
 * 具体 DAL、事务编排、账务处理或余额处理。</p>
 */
package com.wind.funds.governance.projection;
